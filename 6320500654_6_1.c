#include<stdio.h>
#include<string.h>
int main()
{
    int n,i,j,z=65,k,x=1;
    scanf("%d",&n);
    char cha[n];
    int r[n];
    fflush(stdin);
    gets(cha);
    strcmp(cha[])
    puts(cha);
    return 0;
}
